{
  "name": "zkira-pay",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "turbo": "^2.4.0",
    "typescript": "^5.8.0",
    "snarkjs": "^0.7.5"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19",
      "@types/react-dom": "^19"
    },
    "onlyBuiltDependencies": ["esbuild", "sharp", "unrs-resolver", "bufferutil", "utf-8-validate", "bigint-buffer"]
  }
}
